<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ru.cloudinfosys.rc.db.VisitDb">
    <insert id="insertVisit">
        insert into rc_visit values (#{userId}, #{pageId}, #{docDate})
    </insert>

    <select id="getCurrentUniqueUserCount" resultType="_int">
        select	count(distinct userId)
        from	rc_visit a
        where   a.doc_date between date_trunc(now()) and date_trunc(now()) + interval '1 day'
    </select>

    <select id="getCurrentVisitCount" resultType="_int">
        select	count(*)
        from	rc_visit a
        where   a.doc_date between date_trunc(now()) and date_trunc(now()) + interval '1 day'
    </select>
</mapper>